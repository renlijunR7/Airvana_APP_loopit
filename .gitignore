/node_modules
data/*.db
data/*.db-*
.env
*.log
.DS_Store
.idea/
coverage/
.runtime/
android-demo/build/
ios-demo/build/
ios-demo/ios-demo/build/
**/xcuserdata/
*.apk
*.aab
*.idsig
heygen-video-log.jsonl
.gradle/
local.properties
*.keystore
*.jks
captures/
.cxx/
externalNativeBuild/

# 图片重压前的原始资产备份（体积大，不入库；重压脚本可从此回滚）
assets-original/
.playwright-cli/
output/

# 独立打包游戏在 Flutter 侧的副本：由 scripts/sync-flutter-runner.mjs 从
# public/arcade 与 public/assets/featured-originals-v2 生成，不重复入库。
# 构建前执行：node scripts/sync-flutter-runner.mjs
apps/airvana_mobile/assets/arcade/
apps/airvana_mobile/assets/featured-originals-v2/

# Flutter 的 build 在本机是指向 ~/airvana-build 的软链接：
# 项目目录会被同步服务扫描，它给产物反复写 com.apple.FinderInfo 扩展属性，
# 导致 iOS codesign 失败，所以把产物挪到同步范围外。这个软链接是本机配置，
# 绝不能入库——提交上去别人 clone 下来会得到一条指向不存在路径的死链。
apps/airvana_mobile/build
