{
  "name": "neighborhood-gold-rush",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check game/logic.js && node --check game/main.js && node --check service-worker.js",
    "verify": "npm run check && npm test"
  }
}
